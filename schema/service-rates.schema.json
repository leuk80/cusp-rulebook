{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/cusp/cusp-rulebook/schema/service-rates.schema.json",
  "title": "Lodging Service-Charge Rates",
  "description": "Country (ISO 3166-1 alpha-2) → lodging service-charge (\"++\") rate, removed on top of VAT for programs that earn only on the room rate. Decoded as [String: Double]. File convention: `service-rates.json`.",
  "type": "object",
  "propertyNames": { "pattern": "^[A-Z]{2}$" },
  "additionalProperties": { "type": "number", "minimum": 0, "maximum": 1 }
}
